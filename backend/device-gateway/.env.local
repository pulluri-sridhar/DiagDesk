# device-gateway local development env
# Usage: export $(cat .env.local | xargs) && go run ./main.go
# Or:    env $(cat .env.local) go run ./main.go

SERVER_PORT=8084
DB_DSN="postgres://postgres.eahvsmbggamertymxukm:Diagdesk%402026@aws-1-ap-south-1.pooler.supabase.com:5432/postgres?sslmode=require&search_path=devices"
REDIS_ADDR=localhost:6379
KAFKA_BROKERS=localhost:29092
ORDER_SERVICE_URL=http://localhost:8083

# Keycloak — not needed in local dev (device-gateway uses its own token verification)
# Uncomment when running with full auth:
# KEYCLOAK_JWK_URI=http://localhost:8080/realms/diagdesk/protocol/openid-connect/certs
KEYCLOAK_JWK_URI=disabled
