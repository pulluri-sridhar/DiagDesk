# Local development overrides — active when -Dspring.profiles.active=local

spring:
  datasource:
    url: jdbc:postgresql://aws-1-ap-south-1.pooler.supabase.com:5432/postgres?currentSchema=catalog
    username: postgres.eahvsmbggamertymxukm
    password: "Diagdesk@2026"

  data:
    redis:
      host: localhost
      port: 6379
      password: ""

  kafka:
    bootstrap-servers: localhost:29092

  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.oauth2.resource.servlet.OAuth2ResourceServerAutoConfiguration

management:
  tracing:
    sampling:
      probability: 0.0

logging:
  level:
    com.diagdesk: DEBUG
    org.hibernate.SQL: DEBUG
    org.springframework.security: DEBUG
